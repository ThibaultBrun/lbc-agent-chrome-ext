{
  "name": "lbc-bike-analyzer",
  "version": "0.3.0",
  "private": true,
  "description": "Extension Chrome MV3 — analyse vélo Leboncoin avec pipeline IA (Ollama / WebLLM / Gemini Nano).",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.83"
  }
}
